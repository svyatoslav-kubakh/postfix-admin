language: php
sudo: false
php: 7.2
cache:
  bundler: true
  directories:
    - $HOME/.composer
jobs:
  include:
    - stage: PSR-check
      before_script: composer install  --dev
      script: vendor/bin/phpcs --ignore=vendor --extensions=php --standard=PSR2 .
